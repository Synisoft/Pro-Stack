let change = ['apple','ball', 'cat', 'dog', 'elephant']

// change.pop()  // to remove last index
// // change.push('hello') // to incert string in last

// change.shift()//to remove first element
// // change.unshift('alphabet')  // to incert string in first

// let good = change.slice(0, 2)
let good = change.replace('apple', 'alphabet')

console.log(good);




















