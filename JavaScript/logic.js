// function is Armstrong Number(num) {
//     // convert the number to a string to get the number of digits
//     const numStr = num.toString();
//     const numDigits = numStr.length;
  
//     let sum = 0;
//     // loop through each digit of the number and add the cube of the digit to the sum
//     for (let i = 0; i < numDigits; i++) {
//       sum += Math.pow(parseInt(numStr.charAt(i)), numDigits);
//     }
  
//     // if the sum is equal to the original number, it's an Armstrong number
//     return sum === num;
//   }

// console.log(isArmstrongNumber(153));



// let arr = [10, 20, 30, 40, 50, 60]

// let sum = 0;

// for (values of arr) {

//   sum += values
  
// }

// console.log(sum);




// to findex value //


// let arr = [210, 220, 230, 240, 250]
// let array = [210, 220, 230, 240, 250]


// for (size of array) {
  
// }

// console.log(size); 